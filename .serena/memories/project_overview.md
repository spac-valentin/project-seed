# Project Overview

**Name**: project-seed  
**Purpose**: Full-stack seed/template project with an Angular frontend and Java backend.  
**Group**: ro.vspac  
**Version**: 1.0-SNAPSHOT

## Structure
- `ui/` — Angular 21 frontend (TypeScript, TailwindCSS)
- `api/` — Java backend (Gradle, SLF4J)
- `doc/adr/` — Architecture Decision Records
- `shell.nix` / `ui.nix` — Nix dev shells
- `build.gradle` / `settings.gradle` — Root Gradle build
